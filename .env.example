# Copia este archivo como .env y pon tu clave real
# El archivo .env está en .gitignore — nunca se sube a git

# Juez automático de evaluación (Google AI Studio — gratuito, 1500 req/día)
GOOGLE_API_KEY=tu_clave_aqui

# LLM cloud para deploy en HuggingFace Spaces (Groq — gratuito, 6000 tokens/min)
# Obtén tu clave en: https://console.groq.com
# En HF Spaces: Settings > Secrets > GROQ_API_KEY
GROQ_API_KEY=tu_clave_aqui
